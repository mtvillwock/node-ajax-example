# AJAX Example (Node/Express/Mongoose)

## Premise

Example of making an AJAX call to demonstrate interaction between server and client.

To use:

1. Clone this repo: `git clone <url>`
2. `cd` into the directory and run `nodemon app.js`
3. navigate to `localhost:3000`
4. create and delete todos
5. Read through `/public/simple-client.js`
6.